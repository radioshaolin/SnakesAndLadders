# source: https://api.snakes-n-ladders.ga/
# timestamp: Sat Oct 13 2018 21:17:24 GMT+0300 (за східноєвропейським літнім часом)

type Game {
  id: ID!
  players: [Player]
}

type Mutation {
  createGame: Game
  joinGame(id: ID): Game
  createPlayer: Player!
}

type Player {
  id: ID!
  game: Game
}

type Query {
  players(gameId: ID): [Player]
}
