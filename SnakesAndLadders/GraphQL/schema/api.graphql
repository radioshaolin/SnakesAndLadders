
type Game {
  id: ID!
  players: [Player]
}

type Mutation {
  createGame: Game
  joinGame(id: ID): Game
  createPlayer: Player!
}

type Player {
  id: ID!
  game: Game
}

type Query {
  players(gameId: ID): [Player]
}
